# TYPE  DATABASE  USER                 ADDRESS          METHOD
# The Unix socket is reachable only inside the non-root PostgreSQL container.
# Host/network clients always use the SCRAM rules below.
local   all       all                                   trust
host    all       dayorder_migrator    0.0.0.0/0        scram-sha-256
host    all       dayorder_api         0.0.0.0/0        scram-sha-256
host    all       dayorder_worker      0.0.0.0/0        scram-sha-256
host    all       dayorder_monitor     0.0.0.0/0        scram-sha-256
host    replication dayorder_backup    0.0.0.0/0        scram-sha-256
host    all       all                  0.0.0.0/0        reject
host    all       all                  ::0/0            reject
